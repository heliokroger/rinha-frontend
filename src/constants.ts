export const OPENING_BRACKETS = new Set(["{", "["]);
export const CLOSING_BRACKETS = new Set(["}", "]"]);

export const BRACKETS = new Set(["{", "[", "}", "]"]);

export const MAX_FILE_SIZE_FOR_PARSING = 10 * 1_024 * 1_000; // 10mb
